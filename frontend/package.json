{
  "name": "forge-frontend",
  "version": "1.0.0",
  "private": true,
  "description": "Production-ready static frontend for Forge, compatible with Flask templates.",
  "scripts": {
    "build:css": "tailwindcss -i ./css/input.css -o ./css/styles.css --minify",
    "watch:css": "tailwindcss -i ./css/input.css -o ./css/styles.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "3.4.17"
  }
}
